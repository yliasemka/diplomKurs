export enum routes {
    HOME = "/",
    CART = "/cart",
    NEWBOOKS = "/newbooks",
    BOOKDETAILS = "/books/:id",
    FAVORITES = "/favorites",
    ACCOUNT = "/account",
    SEARCH = "/:title/:page",
    SIGNIN = "/sign-in",
    SIGNUP = "/sign-up",
    NOT_FOUND = "*",
}
